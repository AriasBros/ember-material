import Component from '@ember/component';

export default Component.extend({
    /**
     * @since 1.0.0
     * @type {string}
     */
    tagName: "div",

    /**
     * @since 1.0.0
     * @type {string[]}
     */
    classNames: ["slider"]
});
